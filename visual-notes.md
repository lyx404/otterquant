# Visual Notes

## Light mode Landing Page hero
- The hero background image is dark (deep navy), which works well in dark mode
- In light mode, the overlay gradient uses `from-background/60 via-background/40 to-background` 
- The text is dark (foreground) which is hard to read against the dark hero image in light mode
- Need to adjust: either make the overlay stronger in light mode, or use light text on the hero regardless of theme
- The "How It Works" and features sections look good in both modes
- Nav bar shows "Dashboard" button when logged in - correct behavior
