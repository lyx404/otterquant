# 页面模板与结构起点

> 为当前产品选择页面骨架和 App shell；它是结构起点，不是某个示例页面的硬编码。

## 页面类型

- 页面类型：量化研究工作台 + 市场广场 + 策略/交易详情。
- 主要任务：发现、创建、解释、执行和管理量化策略与投资组合。
- 目标用户与使用频率：研究员和交易用户高频使用；账户、订阅与结算按需使用。
- 设备与响应式范围：桌面优先，兼容平板和移动端；移动端以抽屉导航和纵向信息流为主。

## App shell

- 全局导航：`SidebarLayout` 左侧导航，包含探索、创建（因子/策略/模拟交易）和设置。
- 页面标题与上下文：固定页头展示中英文标题与副标题，详情页补充对象上下文。
- 主操作位置：页面首屏标题区或内容卡片右侧；高影响动作使用确认容器。
- 辅助导航、筛选或搜索：广场使用 Explore/My 标签、筛选与搜索；详情页使用分区标签和日期控制。
- 全局反馈与通知：Toaster、通知面板、局部 Alert/错误边界。

## 页面骨架

- 首屏主区：页面标题、关键指标和当前任务入口。
- 次级信息区：趋势图、回撤、版本、运行状态、数据来源与更新时间。
- 操作区：创建、复制、启动/暂停、编辑、导出和订阅等上下文操作。
- 详情/辅助面板：Tooltip、Dialog/Sheet、筛选器、日期范围与通知面板。
- 内容顺序和折叠规则：先结论后证据；移动端次级面板折叠到主内容之后，保持操作可达。

## 信息密度与响应式

- 桌面密度：侧栏 186px（可折叠至 64px），主内容最大宽度约 900–932px，图表优先保证可读高度。
- 移动密度：单列堆叠，触控目标增大，导航转为覆盖式抽屉。
- 关键断点与布局变化：代码已有 `md` 桌面切换；设计稿与工作台 token 另记录约 760px/1180px 的图表与双列堆叠约束，具体断点[待确认]。
- 不得发生的溢出或遮挡：标题、Tooltip、表格和弹层不得被侧栏、视口边缘或固定页头裁切。

## 禁用边界

- 不用于：营销落地页、无侧栏的独立结算页、全屏沉浸式引导页。
- 选择该骨架前必须确认：是否需要持久导航、对象上下文、风险摘要和可恢复操作。

## 策略工作台模板

- 首屏为创建策略主操作，其下是“文件夹导航 + 策略主表”的两栏工作区；文件夹可折叠，主表保持比较、状态与行操作可见。
- 策略主表的列顺序为策略、因子数、表现指标、90 日曲线、最新运行、更新时间与操作；窄视口允许横向滚动，不以压缩文字替代关键列。
- 侧栏底部的新手任务入口打开锚定式浮窗；任务内深链分别进入智能体管理、创建策略和运行策略，关闭后不改变当前页面工作状态。
- 新手引导覆盖当前任务区域但不改变页面骨架；同一时刻仅保留最新触发的一组引导。

<!-- design-spec:managed:start -->
## 项目事实（自动同步）

- 页面/路由线索：[待确认]
- App shell 与布局入口：[待确认]
- 响应式实现线索：[待确认]
<!-- design-spec:managed:end -->
