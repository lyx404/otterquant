# 交互设计 Spec

> 这份声明只定义功能怎样才算成立，不决定视觉风格、组件库或实现路径。

## L1 定位与意图

- 一句话定义：Quandora/Otter Quant 量化交易工作台，帮助用户创建、评估、发布并跟踪因子、策略与投资组合。
- 目标用户：量化研究员、策略开发者、模拟交易与跟投用户。
- 使用场景：探索市场策略、管理我的因子/策略、查看回测与实盘表现、进行模拟交易、管理账户与订阅。
- 非目标：不在前端设计声明中定义交易所撮合、真实资金清算或后端风控实现。
- 行为边界：
  - 始终：保留当前路由上下文、时间窗、数据来源与操作结果。
  - 询问后：创建/编辑策略、启动模拟交易、复制投资组合、账户与订阅变更等可能产生持久影响的动作。
  - 永不：将回测收益呈现为已实现收益，或在数据缺失时伪造确定性结论。

## L2 信息架构

- 页面/屏幕区域：全局侧栏与页头；广场/排行榜/我的；因子与策略工作区；交易与交易详情；账户、订阅与结算。
- 每个区域承担的任务：侧栏负责导航和状态；工作区负责研究与配置；详情页负责解释表现与风险；交易页负责执行状态反馈。
- 区域边界与内容生长规则：内容区随路由变化，桌面端为侧栏 + 主内容列，详情和图表可扩展；移动端侧栏转为抽屉，主任务顺序保持不变。

## L3 核心链路

- 状态清单：空态、加载、成功、错误、禁用、回测、实盘/模拟运行、暂停、完成、权限不足。
- 主链路：进入广场 → 筛选/查看投资组合 → 查看策略详情与图表 → 创建或复制到工作区 → 回到我的策略/交易跟踪。
- 分支链路：因子 → 策略创建 → 回测报告 → 模拟交易；账户 → 订阅/托管 → 结算。
- 可恢复动作：重试、返回上一层、清除筛选、恢复草稿、暂停/继续模拟交易。

## L4 组件功能细节

| 组件/区域 | 功能 | 默认 | 加载 | 禁用 | 错误/成功 |
| --- | --- | --- | --- | --- | --- |
| 侧栏导航 | 切换产品区域、折叠/展开 | 当前路由高亮 | 保持布局骨架 | 无权限项降级 | 路由失败可回退 |
| 策略/组合卡片 | 展示收益、风险与入口 | 可点击、悬停反馈 | 骨架占位 | 不可用操作置灰 | 显示数据更新时间 |
| 图表与 Tooltip | 展示趋势、回撤、NAV 与关键点 | 默认无 Tooltip | 保持图形区域尺寸 | 无数据显示空态 | Tooltip 可关闭/重试 |
| 表单/对话框 | 创建、编辑、确认高影响动作 | 默认可编辑 | 提交中锁定并反馈 | 条件不满足时禁用 | 保留错误字段与修复入口 |

## L5 边界条件

- 空态：说明缺少什么数据，并提供创建、探索或清除筛选的下一步。
- 加载态：优先使用稳定尺寸骨架，避免图表和卡片跳动。
- 错误态：说明受影响范围，提供重试与返回安全页面。
- 权限不足/降级：隐藏高风险动作，保留可读信息并解释原因。
- 网络中断或超时：保留已加载内容，标注数据时间并提供重试。

## L6 验收标准

- Given 用户打开任一受支持路由 / When 页面加载完成 / Then 侧栏、页头、标题上下文与主内容可识别且无溢出。
- Given 图表数据缺失或请求失败 / When 用户查看图表 / Then 显示明确空态/错误态，并可重试而不丢失页面上下文。
- 设计完成定义：核心链路可完成；状态、风险、时间窗和来源可解释；桌面/移动端无裁切；键盘焦点与 reduced motion 有可接受表现。

## 新手引导、列表与文件夹

- 新手引导一次只显示一个；新的引导请求覆盖当前引导，最后一步主按钮统一为“好的”。多步引导操作区固定为进度、跳过/上一步和下一步/好的。
- 新手任务浮窗可通过关闭按钮、`Escape` 或点击浮窗外页面区域关闭；点击浮窗内容与触发按钮本身不应触发外部关闭。
- “创建策略”任务在存在已创建策略时完成；“运行策略”任务在“我的策略”中至少有一个“最新运行 = 已完成”的策略时完成。子任务完成状态独立派生。
- 我的策略工作台以候选、收藏、归档作为文件夹上下文；切换文件夹、筛选、排序、分页或横向滚动不得丢失当前上下文。
- “最新运行”使用已完成、运行中、已排队、待确认、待运行；已排队、待确认与运行中使用同一蓝色语义。

<!-- design-spec:managed:start -->
## 项目事实（自动同步）

- 项目名称：`alpha-mining-platform`
- 项目类型线索：[待确认]
- 入口与路由线索：[待确认]
- 技术栈：`React`, `Vite`, `Tailwind CSS`, `Ant Design`, `Lucide`, `Framer Motion`, `Motion`, `GSAP`
- 可用脚本：`dev: vite --host`, `build: vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist`, `start: NODE_ENV=production node dist/index.js`
<!-- design-spec:managed:end -->
