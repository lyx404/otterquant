# 通用设计工艺声明

> 约束跨业务可复用的排版、层级、反馈、动效和反模板化质量；业务语义写入 `domain.md`。

## 视觉气质与内容节奏

- 视觉气质：暖白/炭黑双主题、Quandora 橙色强调、细边框与克制阴影；圆角形状语言统一。
- 信息密度：研究与交易页面中高密度，关键指标优先；卡片之间保留可扫描的分组留白。
- 内容优先级与留白节奏：页面标题 → 主操作/状态 → 关键指标 → 图表/表格 → 次级说明；避免装饰抢占首屏。
- 需要避免的通用 AI 模板痕迹：无意义渐变、平均化卡片墙、持续运动背景、只用颜色表达状态。

## 排版与层级

- 字体角色：正文 Hanken Grotesk + Noto Sans 系列；标题 Outfit + Noto Sans；数字/代码使用 Roboto Mono。
- 标题/正文/辅助文字层级：标题使用 Outfit 的高对比层级，正文保持紧凑可读，辅助信息降低对比度但不低于可读阈值。
- 中英文与数字排版：中文按本地化字体回退；收益、价格、数量和时间使用等宽数字以便对齐。
- 长文本、截断和换行规则：标题最多两行；策略名、地址等长标识优先中段省略并提供完整 Tooltip。

## 状态反馈与动效

| 触发/时长 | 表现 | 目的 |
| --- | --- | --- |
| 短等待 | 保持布局，不额外闪烁 | 避免闪烁 |
| 长等待 | 稳定尺寸骨架或进度提示 | 建立进度预期 |
| 状态变化 | 使用 transform/opacity 的短反馈，并配合文本 | 说明发生了什么 |

参考等待分档（按项目调整）：`<300ms` 不展示加载；`300ms-2s` 稳定布局的骨架；`>2s` Loader 与进度说明；`>10s` 超时提示与重试入口。

- 动效只服务层级、反馈或状态变化。
- 优先动画 `transform` 和 `opacity`，尊重 reduced motion。
- 每个动效记录目的、触发和结束条件。

## 材质与细节

- 表面、边框、阴影和圆角的整体策略：浅色使用暖白表面和细边框，深色以背景阶层和边框替代重阴影；按钮圆角偏满，卡片约 12–16px。
- 品牌色的使用克制度：橙色只承担主操作、选中、焦点和品牌提示；收益/风险使用独立语义色。
- 空态、错误态和成功态的细节质量：每个状态包含原因、影响和下一步，不只依赖颜色或图标。

## 工艺检查

- [ ] 页面不依赖平均化的等宽卡片墙。
- [ ] 关键层级不靠装饰性 eyebrow 或无意义动效制造。
- [ ] 所有断点、溢出和长文案均有可接受表现。

## 工作台细节

- 新手任务浮窗是紧凑的侧栏附属面板，分组标题与任务步骤保持可扫描密度；步骤环仅在组进度介于 0 和 100 时显示无数字进度环，完成时显示勾选。
- 新手引导浮窗的进度放在操作区左侧，主次操作维持稳定位置，按钮使用 10px 字号与 50px 圆角，避免步骤切换造成布局跳动。
- 策略列表使用表格密度和等宽数值列；状态标签保持短、窄、圆角满值，文字与状态点同色。蓝色运行语义覆盖运行中、已排队、待确认；绿色仅表示已完成。
- 文件夹面板是工作区导航，不使用浮层卡片堆叠；折叠后保留明确展开入口，表格在空间不足时可横向滚动并保留表头语义。

<!-- design-spec:managed:start -->
## 项目事实（自动同步）

- 检测到的字体与样式入口：`package.json`
- 检测到的动效/动画线索：`package.json`
- 响应式断点线索：[待确认]
<!-- design-spec:managed:end -->
