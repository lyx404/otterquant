# 领域语义声明

> 记录只有这个业务领域才成立的对象、状态、风险和责任规则。

## 业务对象与状态

| 业务对象 | 状态/等级 | 用户需要理解什么 | 表达或操作规则 |
| --- | --- | --- | --- |
| 因子 Alpha | 草稿/已发布/归档 | 可复用的量化信号定义 | 发布前需完成配置与验证；归档后不可作为新版本默认入口 |
| 策略 Strategy | 草稿/回测/模拟运行/暂停/完成/失败 | 由因子、规则和参数组成的可执行研究单元 | 回测、模拟与实盘结果必须区分；版本变更需可追溯 |
| 投资组合 Portfolio | 可探索/可复制/已收藏/不可用 | 面向市场展示和跟投的组合 | 展示收益时同时呈现时间窗、风险与数据状态 |
| 交易 Trade | 待执行/部分成交/已成交/失败/取消 | 策略产生的执行记录 | 失败或取消不可伪装为成交；金额、方向和时间需明确 |

## 术语词典

| 术语 | 正式含义 | 禁用/易混表达 |
| --- | --- | --- |
| 回测 | 使用历史数据模拟策略表现 | 不得写成“已实现收益” |
| NAV | 组合净值序列 | 与收益率、回撤分开标注 |
| 回撤 | 从历史高点到当前值的下降幅度 | 不用正收益颜色表达 |
| 模拟交易 | 不触发真实资金的执行演练 | 不得省略“模拟”标识 |
| 跟投/复制 | 用户将公开策略配置到自己的组合 | 需展示风险提示与确认 |

## 风险与责任

- 高风险对象与操作：复制投资组合、启动/停止交易、订阅托管、账户与支付变更。
- 不可逆操作确认规则：提交前展示对象、金额/参数、环境（回测/模拟/实盘）和可逆性；确认后给出结果凭据。
- 风险等级到语义 token 的映射：正向使用 success，警示使用 warning，错误/拒绝使用 destructive；颜色之外必须配合文字或图标。
- 未知或不确定状态的表达：使用“待确认/数据不足/暂不可用”，并保留时间窗与来源。

## 敏感数据

- 默认脱敏字段：API 密钥、访问令牌、支付与身份信息；具体字段规则[待确认]。
- 明文查看条件：仅在用户主动操作且通过权限校验后短时展示；实现细节[待确认]。
- 查看、导出或分享的审计要求：导出与分享应记录对象、时间、操作者和权限范围；后端审计能力[待确认]。

## 领域禁用边界

- 不得把回测、模拟交易或预测表现表达成真实已实现收益。
- 不得省略必要的时间窗、基准、数据状态与来源。

## 策略工作流补充

- 新手任务状态为未完成、进行中或完成，并由实际策略创建、运行和部署状态派生，不得仅按访问次数标记。
- 最新运行状态仅为：`completed`、`running`、`queued`、`awaiting-confirmation`、`awaiting-run`；“待确认”是尚未确认的运行阶段，不能被当作失败或完成。
- “运行策略”新手任务的完成门槛是当前策略集合中至少存在一个 `completed` 最新运行；删除或筛除已完成策略后，任务应重新反映当前集合。
- 文件夹“候选 / 收藏 / 归档”是策略组织维度，不改变策略本身的回测或运行状态。
- 新手任务完成状态与创建策略记录目前保存在浏览器本地存储；跨设备同步与服务端可信来源为[待确认]。

<!-- design-spec:managed:start -->
## 项目事实（自动同步）

- 可观察到的业务/产品关键词：`alpha-mining-platform`
- 代码中出现状态/风险词的文件：`vite.config.ts`
- 可能包含敏感字段的文件：[待确认]
- 以上线索仅供核对，业务语义和责任规则需由项目维护者确认。
<!-- design-spec:managed:end -->
